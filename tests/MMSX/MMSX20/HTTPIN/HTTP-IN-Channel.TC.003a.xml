<test>

		<function name="testName" module="Basic">
			<parameter>HTTP-IN-Channel.TC.003a</parameter>
		</function>
	
		<description>Proxy sends "New-Message-Intercepted" request to Application via HTTPS GET / No enabled Features for Sender and Recipient / Validate the uniqueness of the value of "X-Proxy-Transaction-Id" response header
		<!-- Test Description 
		1. Sender and Recipient both are not provision in application
		2. Send MM1 Message to MMS Proxy 
		3. MMS Proxy should intercept this message and send "New-Message-Intercepted" Request to Application as HTTPS GET Request 
		4. Application check whether Sender and Recipient are registered for the Application Features or not
		5.Validate in then response header the value of "X-Proxy-Transaction-Id" header to MMS should be the unique ID generated by the application for this message to be referenced in the next send-body request -->
		</description>
	
		<!-- Available MSISDN -->
		<configuration>../subscriber_config.xml</configuration>

	<!-- Delete Message Store -->
	<subtest>../deleteMessageStoreAll.xml</subtest>

		<!-- Set mandatory headers -->
		<function name="setHeader" module="HTTP">
			<parameter>X-Proxy-MMS-Message-Type</parameter>
			<parameter>m-send-req</parameter>
		</function>

		<function name="setHeader" module="HTTP">
			<parameter>X-Proxy-Region</parameter>
			<parameter>Siberia</parameter>
		</function>

		<function name="setHeader" module="HTTP">
			<parameter>X-Proxy-Session-Id</parameter>
			<parameter>987654</parameter>
		</function>

		<function name="setHeader" module="HTTP">
			<parameter>To</parameter>
			<configuration>MMSX/NOTINDB/RECIPIENT/MSISDN1</configuration>
		</function>

		<function name="setHeader" module="HTTP">
			<parameter>From</parameter>
			<parameter>MMSX/NOTINDB/SENDER/MSISDN1</parameter>
		</function>

		<function name="setHeader" module="HTTP">
			<parameter>Subject</parameter>
			<parameter>Sample Test</parameter>
		</function>

		<!-- Send Metadeta  Request -->
		<function name="sendGetRequest" module="HTTP">
			<parameter>
				<parameter>https://</parameter>
				<configuration>MMS_Extra/Ip</configuration>
				<parameter>:</parameter>
				<configuration>MMS_Extra/SECUREPORT</configuration>
				<parameter>/New-Message-Intercepted</parameter>
			</parameter>
		</function>

		<function name="checkResponseCode" module="HTTP">
			<parameter>200</parameter>
		</function>

	<!-- check the uniqueness of transaction id -->
		<function name="uniqueCheck" module="Basic">
			<parameter>X-Proxy-Transaction-Id</parameter>
		</function>

</test>